name: Build & Publish Docker Images API

on:
    push:
        branches: [main]
        paths:
            - "api/**"
            - ".github/workflows/api-image.yml"
    pull_request:
        branches: [main]
        paths:
            - "api/**"
            - ".github/workflows/api-image.yml"

permissions:
    contents: read
    packages: write
    id-token: write

env:
    REGISTRY: ghcr.io

jobs:
    build-and-push:
        name: Build & Push (${{ matrix.service }})
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                include:
                    - service: api
                      context: ./api
                      dockerfile: ./api/Dockerfile

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Derive repo/image names
              id: prep
              run: |
                  REPO_LOWER=$(echo "${GITHUB_REPOSITORY}" | tr '[:upper:]' '[:lower:]')
                  OWNER_LOWER=$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')
                  echo "owner_lower=${OWNER_LOWER}" >> $GITHUB_OUTPUT
                  echo "repo_lower=${REPO_LOWER}"   >> $GITHUB_OUTPUT
                  echo "image=${{ env.REGISTRY }}/${OWNER_LOWER}/${REPO_LOWER}-${{ matrix.service }}" >> $GITHUB_OUTPUT

            - name: Compute CalVer
              id: ver
              shell: bash
              run: |
                  # CalVer format: YYYY.MM.DD  (change +%d.%m.%Y if you prefer day-month-year order)
                  CALVER=$(date -u +%Y.%m.%d)
                  BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)
                  SHORT_SHA=${GITHUB_SHA::7}
                  echo "calver=${CALVER}" >> $GITHUB_OUTPUT
                  echo "build_time=${BUILD_TIME}" >> $GITHUB_OUTPUT
                  echo "short_sha=${SHORT_SHA}" >> $GITHUB_OUTPUT

            - name: Set up QEMU
              uses: docker/setup-qemu-action@v3

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to GHCR
              uses: docker/login-action@v3
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Docker meta (labels + tags)
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: ${{ steps.prep.outputs.image }}
                  tags: |
                      type=raw,value=${{ steps.ver.outputs.calver }}
                      type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
                      type=ref,event=branch
                      type=ref,event=pr
                      type=sha
                      type=semver,pattern={{version}},enable=${{ startsWith(github.ref, 'refs/tags/v') }}
                      type=semver,pattern={{major}}.{{minor}},enable=${{ startsWith(github.ref, 'refs/tags/v') }}
                      type=semver,pattern={{major}},enable=${{ startsWith(github.ref, 'refs/tags/v') }}

            - name: Build & Push
              uses: docker/build-push-action@v6
              with:
                  context: ${{ matrix.context }}
                  file: ${{ matrix.dockerfile }}
                  platforms: linux/amd64
                  push: ${{ github.event_name != 'pull_request' }}
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max
                  build-args: |
                      BUILD_VERSION=${{ steps.ver.outputs.calver }}
                      BUILD_SHA=${{ github.sha }}
                      BUILD_SHORT_SHA=${{ steps.ver.outputs.short_sha }}
                      BUILD_TIME=${{ steps.ver.outputs.build_time }}
