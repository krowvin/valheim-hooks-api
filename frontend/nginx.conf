server {
    listen 80;
    server_name _;

    # Serve static assets directly
    location /assets/ {
        try_files $uri =404;
        access_log off;
        expires 1y;
    }

    # SPA fallback: send everything else to index.html
    location / {
        try_files $uri /index.html;
    }

}